{
  "name": "main-docs",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "sync:docs": "bun run scripts/sync-gdocs.ts",
    "sync:yadisk": "bun run scripts/sync-yadisk.ts",
    "test": "bun test",
    "lint": "oxlint --tsconfig=tsconfig.json --deny=correctness scripts",
    "lint:fix": "oxlint --fix --tsconfig=tsconfig.json scripts"
  },
  "devDependencies": {
    "bun-types": "^1.0.10",
    "oxlint": "^1.14.0"
  },
  "dependencies": {
    "effect": "^3.6.5",
    "ky": "^1.9.1"
  }
}
